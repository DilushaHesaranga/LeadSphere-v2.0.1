{
  "name": "leadsphere",
  "private": true,
  "version": "2.0.1",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:mobile": "npm run start --prefix mobile",
    "build": "npm run build --prefix frontend",
    "build:all": "npm run build --prefix frontend && npm run build --prefix backend",
    "test": "npm run test --prefix frontend && npm run test --prefix backend -- --runInBand",
    "test:all": "npm run test && npm run test --prefix mobile",
    "mobile:check": "npm run typecheck --prefix mobile && npm run lint --prefix mobile && npm run test --prefix mobile && npm run validate:config --prefix mobile",
    "firebase:login": "firebase login",
    "firebase:serve": "npm run build && firebase emulators:start --only hosting",
    "firebase:deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^15.25.0"
  }
}
