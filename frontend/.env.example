VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Backend origin only; the client appends /api.
VITE_API_URL=http://localhost:3000

# Set false to hide the optional assistant entry point; backend still enforces enablement.
VITE_AI_ASSISTANT_ENABLED=true
